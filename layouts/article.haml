%article
  %header
    .pull-right
      = tag_links(item[:tags])
      %span.icon-time
      = item[:created_at]
    %h4
      %span{ :class => "icon-#{item[:icon] || 'calendar'}" }
      = link_to item[:title], item.path
  = find_and_preserve(item.compiled_content)
